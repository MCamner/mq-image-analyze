# Test Fixtures

Real image fixtures for integration tests.

Add sample images here when testing vision pipelines end-to-end.
Synthetic images are generated programmatically in `conftest.py` for unit tests.

Fixture files are gitignored if large (`.jpg` > 1MB, model files).
