name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        # Same constraint as tests.yml: a release must not be cut against a
        # resolution the test job never saw.
        run: |
          pip install -c constraints.txt -e ".[dev,web,mcp]"
          python -m pip install build

      - name: Run release checks
        run: |
          bash release-check.sh

      - name: Build package
        run: |
          python -m build

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --title "${GITHUB_REF_NAME}" \
            --notes-file CHANGELOG.md \
            dist/*
